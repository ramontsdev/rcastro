/** Configuração central da marca e SEO (única fonte de verdade). */

export const siteName = 'RCastro'

export const siteDescription =
  'Grife de bolsas feitas à mão, numeradas e em edição limitada. Linhas em crochê e couro sintético premium.'

/** Título da home quando nenhuma página filha define `title`. */
export const siteDefaultTitle = 'RCastro | Bolsas artesanais numeradas'

/** Sufixo aplicado às páginas internas via `metadata.title` + template no layout. */
export const siteTitleTemplate = `%s | ${siteName}`

export const contactEmail = 'contato@rcastro.com.br'
